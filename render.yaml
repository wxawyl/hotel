services:
  - type: web
    name: hotel-app
    env: docker
    plan: starter
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "5000"
      - key: JWT_SECRET
        value: "your-secure-jwt-secret-key-here-change-this-in-production"